Ejercicio 1.

Ana Pérez: valido
Carlos Gómez: valido
12345: invalido
María López: valido
Juan_123: invalido

¿Cuántas veces se ejecuta validar_nombre()?
* 5 veces ya que se valida en cada interacción, es decir al digitar cada uno de los 5 nombres se realiza un analizas sin importar si el resultado es valido o invalido

¿Cuántas veces se ejecuta el if?
* "if" se ejecuta cinco veces 

¿Qué parte cambia en cada iteración?/¿Qué parte permanece igual?
* existen dos formas de clasificación, Nombre valido e invalido, por lo que dentro de estos dos rangos el cambio depende del nombre o texto que ingresemos, si tenemos en cuenta el nombre Ana pérez y lo comparamos con Carlos Gómez, ambos son diferentes pero hacen parte de la agrupación de nombres validos, a diferencia de 12345 el cual no es un nombre propio ya que usa caracteres que no pertenecen

Ejercicio 2.
ana@gmail.com: valido
correo: invalido
usuario@dominio.com: valido
@test.com: invalido
usuario@: invalido

¿Cuántas veces se ejecuta validar_correo()?
*Se ejecuta 5 veces. Ocurre una vez por cada vuelta del bucle

¿Cuántas veces se ejecuta el if?
*Se evalúa 5 veces. En cada una de las 5 iteraciones se evalúa la condición

¿Qué parte cambia en cada iteración?/¿Qué parte permanece igual?
*La variable del bucle i (toma los valores 0, 1, 2, 3, 4).

*El texto del mensaje del input() (muestra 1, 2, 3, 4, 5 gracias a i + 1)
*La variable correo (almacena la entrada de texto introducida por el usuario en cada vuelta).
*El resultado devuelto por validar_correo(correo) (True o False) y, por lo tanto, la rama de la estructura condicional (if o else) que termina ejecutándose.
Permanece igual:
La estructura del bucle for y la llamada a la función validar_correo()

La lógica del condicional if...else.

Los mensajes fijos de salida: "Correo válido" y "Correo inválido"

Ejercicio 3.

0: valido
1: valido
50: valido
99: valido
100: valido
101: invalido
-1: invalido

¿Cuántas veces se ejecuta validar_rango()?
*Se ejecuta 5 veces. Ocurre una vez por cada iteración del bucle

¿Cuántas veces se ejecuta el if?
*Se evalúa 5 veces. En cada vuelta del bucle se comprueba la condición

¿Qué parte cambia en cada iteración?
*La variable del bucle i (toma los valores 0, 1, 2, 3, 4)
*El número de la iteración mostrado en el mensaje del input() (1, 2, 3, 4, 5)
*La variable valor, que almacena el número ingresado por el usuario en cada vuelta (0, 1, 50, 99, 100)

¿Qué parte permanece igual?
La estructura del bucle for y la llamada a la función validar_rango().

*Los límites fijos pasados a la función (0 y 100)
*La lógica del condicional if...else y sus mensajes de texto impresos.

Ejercicio 4.
Estudiante: valido
Docente: valido
Administrativo: valido 

¿Cuántas veces se ejecuta validar_opcion()?
*se ejecuta 3 veces (hasta el momento). Cada vez que el usuario ingresa un valor válido, la función se llama una vez. Para que se ejecute las 5 veces totales del bucle, el usuario tendría que ingresar las 2 entradas faltantes.

¿Cuántas veces se ejecuta el if?
*Se evalúa 3 veces (hasta el momento). Se evalúa en cada iteración justo después de recibir la entrada del usuario. En los tres casos evaluó a True, por lo que imprimió "Opción válida" 3 veces

¿Qué parte cambia en cada iteración?
*La variable del bucle i (toma los valores 0, 1, 2 en estas primeras iteraciones).
*La variable opcion, que recibe el número ingresado en cada vuelta (1, 2, 3).
*El valor que recibe validar_opcion(opcion) como argumento.

¿Qué parte permanece igual?
*El mensaje del input(): "Seleccione una opción (1, 2 o 3): " (a diferencia de los códigos anteriores, este no usa i + 1 en el texto).
*La lista fija contra la que se compara dentro de la función: [1, 2, 3].
*La lógica del if...else y sus mensajes impresos ("Opción válida" / "Opción inválida").

Ejercicio 5:
Este programa solicita al usuario ingresar 10 correos electrónicos. Utiliza la función validar_correo()para verificar si cada correo cumple con una estructura válida. Si el correo es válido, aumenta en uno el contador de correos válidos.
En caso contrario, aumenta el contador de correos inválidos. Al finalizar el ciclo, se muestran las cantidades totales de correos válidos e inválidos procesados.
 from validaciones import validar_correo

validos = 0
invalidos = 0

for i in range(10):
    correo = input(f"Ingrese el correo {i + 1}: ")

    if validar_correo(correo):
        validos += 1
    else:
        invalidos += 1

print("Correos válidos:", validos)
print("Correos inválidos:", invalidos)

Ejercicio 6:
Este programa clasifica diez valores numéricos en tres categorías: BAJO, NORMAL y ALTO. Para hacerlo utiliza la función clasificar_valor(), que analiza el valor ingresado y devuelve la categoría correspondiente.
Durante el procesamiento se llevan contadores para cada categoría y al finalizar se muestra la cantidad de datos encontrados en cada una.

def clasificar_valor(valor):
    if valor < 60:
        return "BAJO"
    elif valor < 80:
        return "NORMAL"
    else:
        return "ALTO"

bajo = 0
normal = 0
alto = 0

for i in range(10):
    valor = float(input(f"Ingrese el valor {i + 1}: "))

    categoria = clasificar_valor(valor)

    print("Categoría:", categoria)

    if categoria == "BAJO":
        bajo += 1
    elif categoria == "NORMAL":
        normal += 1
    else:
        alto += 1

print("BAJO:", bajo)
print("NORMAL:", normal)
print("ALTO:", alto)

Ejercicio 7:
Este programa procesa ocho nombres utilizando la función validar_nombre(). Los nombres válidos e inválidos se contabilizan mediante contadores. Posteriormente se calcula el porcentaje de nombres válidos con respecto al total de registros procesados.
Finalmente se muestran los resultados obtenidos.

from validaciones import validar_nombre

validos = 0
invalidos = 0
total = 8

for i in range(total):
    nombre = input(f"Ingrese el nombre {i + 1}: ")

    if validar_nombre(nombre):
        validos += 1
    else:
        invalidos += 1

porcentaje = (validos / total) * 100

print("Válidos:", validos)
print("Inválidos:", invalidos)
print("Porcentaje de calidad:", porcentaje, "%")

Ejercicio 8:
Este programa valida diez edades utilizando la función validar_rango(). Una edad será considerada válida únicamente si se encuentra dentro del rango de 0 a 120 años.
El programa contabiliza la cantidad de edades válidas e inválidas y presenta un resumen al finalizar.

from validaciones import validar_rango

validas = 0
invalidas = 0

for i in range(10):
    edad = int(input(f"Ingrese la edad {i + 1}: "))

    if validar_rango(edad, 0, 120):
        validas += 1
    else:
        invalidas += 1

print("Edades válidas:", validas)
print("Edades inválidas:", invalidas)

Ejercicio 9:
Este programa procesa cinco registros de usuario. Cada registro contiene nombre, correo electrónico y edad. Se realizan validaciones independientes para cada campo y solamente cuando todas las validaciones son correctas el registro es considerado válido.
En caso de que alguna validación falle, el registro será marcado como inválido.

from validaciones import validar_nombre, validar_correo, validar_rango

for i in range(5):

    print(f"Registro {i + 1}")

    nombre = input("Nombre: ")
    correo = input("Correo: ")
    edad = int(input("Edad: "))

    nombre_valido = validar_nombre(nombre)
    correo_valido = validar_correo(correo)
    edad_valida = validar_rango(edad, 0, 120)

    if nombre_valido and correo_valido and edad_valida:
        print("Registro válido")
    else:
        print("Registro inválido")

Ejercicio 10:
Este programa analiza la calidad de cinco registros mediante la validación de cuatro campos: nombre, correo electrónico, edad y valor. Cada campo debe cumplir una regla específica. Solamente los registros cuyos campos cumplen todas las condiciones se consideran válidos.
El sistema contabiliza registros válidos e inválidos y muestra un resumen final.

from validaciones import validar_nombre, validar_correo, validar_rango

total = 5
validos = 0
invalidos = 0

for i in range(total):

    print(f"\nRegistro {i + 1}")

    nombre = input("Nombre: ")
    correo = input("Correo: ")
    edad = int(input("Edad: "))
    valor = float(input("Valor: "))

    nombre_valido = validar_nombre(nombre)
    correo_valido = validar_correo(correo)
    edad_valida = validar_rango(edad, 0, 120)
    valor_valido = validar_rango(valor, 0, 100)

    if nombre_valido and correo_valido and edad_valida and valor_valido:
        validos += 1
        print("Registro válido")
    else:
        invalidos += 1
        print("Registro inválido")

print("\nResumen")
print("Total:", total)
print("Válidos:", validos)
print("Inválidos:", invalidos)

Ejercicio 11:
El indicador de calidad permite medir qué porcentaje de registros procesados son válidos. Si de 20 registros analizados, 17 cumplen todos los criterios de validación,
la calidad obtenida será del 85 %, lo que indica un nivel alto de calidad en los datos procesados.
La fórmula es:


calidad = (validos / total) * 

Si existen:

validos = 17
total = 20

Entonces:


calidad = (17 / 20) * 100

calidad = 0.85 * 100
calidad = 85

Respuesta:
Calidad de los datos: 85 %
